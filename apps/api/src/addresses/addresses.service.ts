import { Injectable } from '@nestjs/common';

/**
 * Faz 2 — BACKEND_PLAN §5.4. Adres CRUD; her sorgu userId ile scope'lanır
 * (IDOR önleme — ENGINEERING_STANDARDS §4).
 */
@Injectable()
export class AddressesService {}

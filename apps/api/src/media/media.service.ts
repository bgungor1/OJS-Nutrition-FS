import { Injectable } from '@nestjs/common';

/**
 * Faz 4 — BACKEND_PLAN §7. Görsel yükleme (multer, yerel disk / Render
 * Persistent Disk). Sunum ServeStaticModule ile /media altında (app.module.ts).
 * CDN/S3 soyutlaması eklenmez (YAGNI).
 */
@Injectable()
export class MediaService {}

import { Injectable } from '@nestjs/common';

/**
 * Faz 3 — BACKEND_PLAN §5.8. GET /faq?category= -> FaqItem[].
 * Admin: POST/PUT/DELETE /faq. Seyrek değişir; kısa TTL cache adayı.
 */
@Injectable()
export class FaqService {}
